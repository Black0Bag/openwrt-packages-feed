# 同步规则: luci-app-cloud-clipboard
# 上游: Jonnyan404/cloud-clipboard-go
# 说明: 云剪贴板 LuCI 界面
#
# 可配置字段:
#   source_repo   - 上游仓库（必填）
#   source_path   - 上游仓库中的路径，空字符串表示根目录
#   target        - 本仓库中的目标目录（必填）
#   exclude       - 排除的文件/目录列表（可选，按顶层名匹配）
#   description   - 描述信息（可选，仅用于日志）
#   branch        - 指定分支（可选，留空则自动获取上游默认分支）

source_repo: "Jonnyan404/cloud-clipboard-go"
source_path: "openwrt/luci-app-cloud-clipboard"
target: "luci-app-cloud-clipboard/"
exclude: []
description: "云剪贴板 LuCI 界面"
branch: ""
